version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "Hello Bala, This is a test variable!!!"

steps:
  - type: Command
    name: "Dockerize"
    command: |
      echo "Hello Bala" > docktest-result.txt
artifacts:
  - name: docktest-result
    type: BINARY
    files:
      - docktest-result.txt
